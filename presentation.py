import streamlit as st
import reveal_slides as rs


def presentation_page():
    st.title("📽 Презентация проекта", anchor=False)
    st.markdown("Обзор прогнозирования отказов оборудования с использованием Random Forest.")

    slides_content = """
    # Прогнозирование отказов оборудования

    ---

    ## 🎯 Цели проекта
    - **Задача**: Предсказать отказ оборудования (Target = 1) или его отсутствие (Target = 0).
    - **Данные**: 10,000 записей, 14 признаков (температура, скорость, крутящий момент и др.).

    ---

    ## 🛠 Этапы работы
    1. Загрузка и очистка данных
    2. Масштабирование числовых признаков
    3. Обучение модели Random Forest
    4. Оценка: Accuracy, ROC-AUC, Confusion Matrix
    5. Интерактивный интерфейс для прогнозов

    ---

    ## 🧹 Предобработка
    - Удалены столбцы: `UDI`, `Product ID`, типы отказов.
    - Кодирование `Type`: L→0, M→1, H→2.
    - Применен `StandardScaler` для числовых признаков.

    ---

    ## 🤖 Модель
    - **Алгоритм**: Random Forest (100 деревьев).
    - **Разделение данных**: 80% обучение, 20% тест.
    - **Метрики**:
        - Accuracy: ~0.95
        - ROC-AUC: ~0.97

    ---

    ## 📈 Результаты
    - Интерактивная матрица ошибок и ROC-кривая в интерфейсе.
    - Высокая точность предсказаний.
    - Прогноз вероятности отказа для новых данных.

    ---

    ## 🖱 Интерфейс
    - **Ввод параметров**:
        - Тип оборудования (L, M, H)
        - Температура воздуха и процесса
        - Скорость вращения, крутящий момент, износ
    - **Вывод**:
        - Класс (0 или 1)
        - Вероятность отказа (с прогресс-баром)

    ---

    ## 🚀 Перспективы
    - Добавить модели: XGBoost, LightGBM, SVM.
    - Автоматическая настройка гиперпараметров.
    - Интеграция с UCI через `ucimlrepo`.
    - Расширение визуализаций (динамические дашборды).

    """

    rs.slides(slides_content, theme="dracula", height=700, config={"backgroundTransition": "zoom"})